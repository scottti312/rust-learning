pub mod operations {
    pub fn add_one(x: i32) -> i32 {
        x + 1
    }
    pub fn subtract_one(x: i32) -> i32 {
        x - 1
    }
}

pub struct Options {
    pub this: String,
    pub test: i32,
}
